package css

const (
	Debug    = "debug"
	DebugAll = "debug *"
)

templ StylesUtilDebug() {
	<style>
		.debug,
		.debug * {
			border: var(--ui-border-width) var(--ui-border-style) red;
			border-radius: var(--ui-radius);
		}
	</style>
}
