{
    "name": "ui",
    "version": "0.2.0",
    "description": "Just an ui lib for my projects.",
    "main": "src/index.js",
    "style": "dist/main.min.css",
    "files": [
        "LICENSE",
        "src",
        "dist",
        "svg"
    ],
    "repository": "knackwurstking/ui",
    "scripts": {
        "build": "npm run build:lib && npm run build:css",
        "build:lib": "vite build --minify -c vite.config.js",
        "build:css": "cleancss -o dist/main.min.css css/main.css",
        "build:types": "rm src/**/*.d.ts && npx -p typescript tsc src/*.js src/**/*.js  --emitDeclarationOnly --declaration --allowJs"
    },
    "license": "MIT",
    "devDependencies": {
        "clean-css-cli": "^5.6.3"
    },
    "dependencies": {
        "vite": "^5.2.9"
    }
}
