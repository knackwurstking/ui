{
    "name": "ui",
    "version": "0.2.0",
    "description": "Just an ui lib for my projects.",
    "type": "module",
    "main": "src/index.js",
    "style": "css/main.css",
    "files": [
        "LICENSE",
        "src",
        "dist",
        "svg"
    ],
    "repository": "knackwurstking/ui",
    "scripts": {
        "build": "npm run build:lib && npm run build:css",
        "build:lib": "vite build --minify -c vite.config.js",
        "build:css": "cleancss -o dist/main.min.css css/main.css",
        "build:types": "npx -p typescript tsc src/*.js src/**/*.js  --emitDeclarationOnly --declaration --allowJs"
    },
    "license": "MIT",
    "devDependencies": {
        "clean-css-cli": "^5.6.3"
    },
    "dependencies": {
        "vite": "^5.2.9"
    }
}
