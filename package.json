{
  "name": "ui",
  "version": "0.2.0",
  "description": "Just an ui lib for my projects.",
  "main": "src/index.js",
  "types": "types/index.d.ts",
  "style": "main.min.css",
  "files": [
    "LICENSE",
    "css",
    "src",
    "index.js"
  ],
  "repository": "knackwurstking/ui",
  "scripts": {
    "build:css": "cleancss -o main.min.css css/main.css",
    "build:types": "npx -p typescript tsc src/**/*.js --declaration --allowJs --emitDeclarationOnly --outDir types"
  },
  "license": "MIT",
  "devDependencies": {
    "clean-css-cli": "^5.6.3"
  }
}
